---
title: Google Style Reference
description: Explore the Google C++ Style Guide requirements followed in CSCE240, including naming conventions, formatting rules, and best practices.
lastUpdated: true
---

import { Aside, Badge, Code, TabItem, Tabs } from "@astrojs/starlight/components";
import { styleGuide } from "@ts/references/style";

This page outlines the **correct styling practices** for scenarios encountered in the course, 
based on the **Google C++ Style Guide**. Each section provides clear examples to ensure your 
code adheres to the required standards, helping you write clean, readable, and maintainable 
programs. Use this as a guide to meet the course's styling expectations.

## `Access Specifier Indent` <Badge text="Detected" variant="success"  size="medium" aria-label="error detected"/>
Access specifiers like `public`, `protected`, and `private` should be indented by 1-space. You should NOT have a 
blank line after an access specifier.
<Code lang="cpp" code={styleGuide.accessSpecifier} showLineNumbers={false} />
[Reference to `cpplint` error](./cpplint-reference#access-specifier-indent)

## `Class Blank Space` <Badge text="Detected" variant="success"  size="medium" aria-label="error detected"/>
There should NOT be a blank line after the class name line or before the closing brace line.
<Code lang="cpp" code={styleGuide.classBlankSpace} showLineNumbers={false} />
[Reference to `cpplint` error](./cpplint-reference#class-blank-space)

## `Class Data Member` <Badge text="Not Detected" variant="danger"  size="medium" aria-label="error not detected"/>
The name of the data member should ALWAYS end with an underscore (`_`).
```cpp showLineNumbers=false
int data_;
string name_;
```
Error not detected by `cpplint`

## `Class Name` <Badge text="Not Detected" variant="danger"  size="medium" aria-label="error not detected"/>
The name of your class should be capitalized, and should follow CamelCase rules.
```cpp showLineNumbers=false
class MyCamelClass {
  // Members
};
```
Error not detected by `cpplint`

## `Column Length` <Badge text="Detected" variant="success"  size="medium" aria-label="error detected"/>
Your **MAX** column length is 80.
<Code lang="cpp"  code={ styleGuide.columnLength }/>
[Reference to `cpplint` error](./cpplint-reference#column-length)

## `Constant` <Badge text="Not Detected" variant="danger"  size="medium" aria-label="error not detected"/>
Constants should be formatted with a lowercase `k` followed by `CamelCasing` the rest of the name.
```cpp showLineNumbers=false
const double kPie = 3.14;
```
Error not detected by `cpplint`

## `Control Structures` <Badge text="Detected" variant="success"  size="medium" aria-label="error detected"/>

- Blank space between the statement type and the parenthesis.

- No blank spaces after the opening parenthesis and before the closing parenthesis.

- Single space between the parenthesis and the opening brace.

- Opening brace should be on the same line as the statement.

- No blank lines between the braces and the code block.

### `Do-While`

<Code lang="cpp" code={styleGuide.doWhileBraces} showLineNumbers={false} />

### `For`

<Code lang="cpp" code={styleGuide.forLoops} showLineNumbers={false} />

### `If-Else`

<Tabs>

<TabItem label="If" icon="seti:cpp">
    <Code lang="cpp" code={styleGuide.ifNoElse} showLineNumbers={false} />
</TabItem>

<TabItem label="If-Else" icon="seti:cpp">
    <Code lang="cpp" code={styleGuide.ifWithElse} showLineNumbers={false} />
</TabItem>

<TabItem label="Nested" icon="seti:cpp">
    <Code lang="cpp" code={styleGuide.ifWithNested} showLineNumbers={false} />
</TabItem>

</Tabs>

### `Switch`

<Code lang="cpp" code={styleGuide.switchBraces} showLineNumbers={false} />

### `While`

<Code lang="cpp" code={styleGuide.whileBraces} showLineNumbers={false} />

[Reference to `cpplint` errors](./cpplint-reference#control-structures)

## `Copyright` <Badge text="Detected" variant="success"  size="medium" aria-label="error detected"/>
The first line of each file should be authored with a comment in the following format `// Copyright <year> <name>`.
```cpp showLineNumbers=false
// Copyright 2024 CSCE240
```
[Reference to `cpplint` error](./cpplint-reference#copyright)

## `Function` <Badge text="Mostly Detected" variant="caution"  size="medium" aria-label="error mostly detected"/>
- The name of the function should start with a capitalized letter and casing should follow CamelCase rules. This is not caught by `cpplint`.

<Aside>The `main` function is an exception and should **ALWAYS** be written as `main()`.</Aside>

- Parenthesis should be next to the function's name.

- There should **NOT** be a space after the opening parenthesis, or before the ending parenthesis.

- There should be a single space between the opening parenthesis, and the opening brace.

- Opening braces should **ALWAYS** be in the same line as the associated statement. If the function implementation is only 
one small line, you can place the closing brace on the same line.

- There should **NOT** be an empty blank line after the function declaration or before the closing brace of the function.

<Tabs>

<TabItem label="Single-Line" icon="seti:cpp">
    <Code lang="cpp" code={ styleGuide.funcBraces1 } showLineNumbers={false} />
</TabItem>

<TabItem label="Multi-Line" icon="seti:cpp">
    <Code lang="cpp" code={ styleGuide.funcBraces2 } showLineNumbers={false} />
</TabItem>

</Tabs>

[Reference to `cpplint` error](./cpplint-reference#function)

## `Header Guards` <Badge text="Falsely Detected" variant="success"  size="medium" aria-label="error falsely detected"/>
You can disregard the error thrown as `cpplint` asks for a complete file path after `#ifndef` and after 
`#endif`. Complete filepaths should **NOT** be used in this course.
<Code lang="cpp" code={ styleGuide.headerGuards } showLineNumbers={false} />
[Reference to `cpplint` error](./cpplint-reference#header-guards)

## `Include File` <Badge text="Falsely Detected" variant="success"  size="medium" aria-label="error falsely detected"/>
You can disregard the error thrown as `cpplint` asks for a complete file path after `#include` instead of the 
relative one that is used. Complete filepaths should **NOT** be used in this course.
<Code lang="cpp" code={ styleGuide.includeFile } showLineNumbers={false} />
[Reference to `cpplint` error](./cpplint-reference#include-file)

## `Indentation` <Badge text="Not Detected" variant="danger"  size="medium" aria-label="error not detected"/>
2-space indent length per indentation level.
<Code lang="cpp" code={ styleGuide.indentation } />
Error not detected by `cpplint`

## `Makefile` <Badge text="Detected" variant="success"  size="medium" aria-label="error detected"/>
Your recipe commands should be indented with a `tab` not spaces. Copying and pasting code snippets may 
produce this error as some code editors don't recognize a copied `tab`.
<Code lang="cpp" code={ styleGuide.makeFile } showLineNumbers={false} />

[Reference to `cpplint` error](./cpplint-reference#makefile)

## `Newline Character` <Badge text="Detected" variant="success"  size="medium" aria-label="error detected"/>
Every file should end with a blank line.

[Reference to `cpplint` error](./cpplint-reference#newline-character)
<Aside type="caution">Coding snippets do not provide an empty line at the end.</Aside>

## `Variable` <Badge text="Not Detected" variant="danger"  size="medium" aria-label="error not detected"/>
Variables should be named using `snake_case`.
```cpp showLineNumbers=false
int my_integer = 10;
double my_double = 10.0;
```
Error not detected by `cpplint`